// javascript document

var main = {
    info : {
    },
    create : function(){
        if ( main.created ){
            return false;
        }

        main.created = true;
        return true;
    },
    itemLoad : function(container, query){
        var a; // source connect
    }
}
